# 01_可扩展数据库类型_任务清单

> 目标：第三方开发者不改本仓库即可发包增加数据库类型。
> 每条任务都标注产出物与验收方式；AC 编号对应需求文档的验收标准。

## M0 — 文档与元数据（已完成）

- [x] 建立 `docs/` 文档区与索引
- [x] 编写 PRD（三类开发者、用户故事、验收标准）
- [x] 编写设计方案（能力声明 / 配置字段 / 分发）
- [x] 编写方言作者 API 契约
- [x] 记录配置字段方案选型决策
- [x] 写入 `project-meta.json`

产出：`docs/` 全部文件。

## M1 — 打通分发（P0）

- [x] `package.json` 补 `peerDependencies`：`@deepseek-ai/cordis` 及用到的 harness 包
- [x] 全部标注 `peerDependenciesMeta.optional`（未发布的包不能由包管理器解析，见决策记录）
- [ ] **在打包版 dsh 上实测 `dsh plugin --profile <name> add .`** ← 唯一未完成项，本机无打包版 dsh
- [ ] 补一个「安装后可加载」的回归用例

验收：**AC4**。声明已落地，安装实测待打包版 dsh 环境。

## M2 — 能力声明与工具降级（P0）✅

- [x] 定义 `DialectCapability` 与 `capabilities` 字段
- [x] 注册时校验能力键，未知键抛错
- [x] `db_describe`：`createStatement` 缺失时省略、`indexes` 缺失时为空数组
- [x] `db_tables`：`estimatedRows` 缺失时为 `null`
- [x] `db_databases`：`charset` 缺失时为空串
- [x] 输出 schema 中对应字段改为可选
- [x] 自带 MySQL 方言声明全部能力，`verify:host` 断言行为不变
- [x] 用一个「缺能力的桩方言」覆盖降级路径

验收：**AC2**、**AC7**。

## M3 — 配置字段扩展（P0）✅

- [x] `ConnectionProfile` 增加 `extra: Record<string, string | number>`，缺失按 `{}`
- [x] 定义 `DialectConfigField` 与方言的 `configFields`
- [x] 宿主 schema 接受 `extra`（`z.dict`）
- [x] 新增 `GET /api/ds-db/dialects` 路由，页面类型选择来自真实注册表
- [x] 设置页弹窗按选中方言渲染额外字段并标记 `required`
- [x] 字典回退：字段自带 `label`
- [ ] 旧连接（无 `extra`）无需重新录入的回归用例 ← 由 schema 默认值覆盖，未单独写用例

验收：**AC3**、**AC6**。

## M4 — 方言包化验证（P1，验证 M1–M3）✅

- [x] 方言包化验证：`dialects/mysql` 与第三方方言形状完全一致，通过 `inject: ['databaseDialects']` 注册
- [x] `verify:host` 改为**以独立插件挂载方言包**，据此跑通全部断言（工具注册、描述文本、拒绝文本、能力降级、目录）
- [x] `dsh web` 实际启动验证：方言行与核心行分别加载、无警告
- [x] 起手骨架 `dialects/_template`（仓内/出仓两用），契约自检通过

验收：**AC1**。「第三方能扩展」由方言工作区 + `verify:host` 的挂载方式证实。

> 原 M4 的 PostgreSQL 实现按方案 A 取消：`examples/dsh-dialect-postgres` 已移除，`dialects/mysql` 承担"完整真实实现"的角色。真要加 PostgreSQL 方言，照 `dialects/_template` 起手即可（`information_schema` 大体可移植、`?`→`$n` 是机械替换）。

## M5 — 方言作者 SDK 与自检套件（P1）✅ 主体完成

- [x] 导出 `auditDialect(dialect)`：只读判定（含字面量/注释绕过）、上界、标识符引用、投影、能力与方法一致性、字段默认值类型
- [x] 本插件自带 MySQL 方言复用该套件（`npm test` 24 例全绿）
- [x] 文档：方言作者 API 契约 + 示例包
- [x] 模板骨架 `dialects/_template`（可直接复制，仓内或出仓两用）
- [ ] 独立模板仓库 `dsh-dialect-template`（把骨架抽成独立仓，便于第三方 fork）

验收：**AC5**。

## M6 — 工具集开放与呈现（P2）部分完成

- [x] `db_sample`（采样）、`db_explain`（执行计划）作为可选能力，按方言能力注册
- [x] 未提供该能力的方言：工具根本不注册（模型工具列表里看不到）
- [ ] Web 侧查询结果表格卡片
- [ ] 调用卡片（presentCall / presentResult）

验收：**AC7** + 人工验收。

## 各阶段通用门禁

每完成一个里程碑，必须全绿：

```sh
npm run typecheck
npm test
npm run verify:host
npm run build
```

并在源码态与打包态各启动一次 dsh 冒烟。

## 暂不做（P3，单独评估）

- 写操作：需审批链路与写账号，属产品形态变化
- Oracle：无 `LIMIT`、无 `information_schema`、`oracledb` 为重量级原生依赖
